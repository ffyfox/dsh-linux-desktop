# 依赖与构建产物
node_modules/
dist/
*.tgz
*.log

# 编辑器 / 系统
.DS_Store
.idea/
.vscode/
*.swp

# 文件管理器（Dolphin）给目录设置自定义图标时写下的文件。
# 它记录的是**本机绝对路径**，既不该进仓库，也不该成为别处的引用来源。
.directory

# 本项目的临时测试沙箱（隔离测试用）
.sandbox/
tmp/
